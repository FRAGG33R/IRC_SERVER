#ifndef CLASS_JOIN_HPP
#define CLASS_JOIN_HPP
#include <string>
#include <vector>

class Join
{
	private:
		std::string __channels;
		std::string __keys;
	public:
		Join();
		// void	set_channels_keys(std::vector<std::string> __params);
};
#endif